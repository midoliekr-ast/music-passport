# Music Passport 디자인 가이드

> **문서 상태:** MVP 디자인 방향 초안  
> **기준 문서:** `PRD.md`  
> **목적:** Music Passport의 디자인 철학, 화면 구조, UI 패턴 및 반응형·접근성 원칙을 정의한다.  
> **제품 슬로건:** **Music Passport — 음악으로, 세계를 여행하다.**

## 1. 디자인 비전

Music Passport는 일반적인 음악 스트리밍 앱이 아니라, 사용자가 음악을 매개로 한 도시의 문화에 입국하고 짧은 여행 기록을 발급받는 경험이다. 화면은 **여권의 질서**, **여행의 설렘**, **독립 잡지의 편집 감각**, **종이의 물성**을 현대적이고 절제된 방식으로 결합한다.

사용자는 `Mood → Situation → Destination`을 선택하며 출국을 준비하고, 결과 화면에서 Music Passport와 다섯 곡의 Playlist Card를 받는다. Spotify는 이 경험의 브랜드 중심이 아니라 실제 음악을 듣기 위한 명확한 외부 동선으로만 사용한다.

### 1.1 핵심 키워드

- **Passport:** 정보의 신뢰감, 발급물의 소장 가치, 명확한 필드 구조
- **Travel:** 단계별 여정, 목적지 중심 탐색, 도착의 성취감
- **Immigration Stamp:** 도시 방문의 흔적과 세션 내 수집 경험
- **Editorial Design:** 강한 타이포그래피 위계, 그리드, 번호 체계, 캡션
- **Paper Texture:** 미세한 재질감과 겹침으로 표현하는 물성
- **Playlist Card:** 다섯 곡을 한정판 인쇄물처럼 편집한 결과물
- **Minimal / Modern:** 장식보다 정보, 넉넉한 여백, 절제된 인터랙션
- **Cultural Discovery:** 이국적 장식이 아니라 구체적인 음악 문화 맥락의 발견

### 1.2 지향하는 인상

- 깔끔하고 현대적인 한국 여권이 주는 정돈감과 품격을 참고한다.
- 작은 음악 문화지처럼 지적이고 편집 의도가 분명해야 한다.
- 새 종이와 오래 간직한 여행 기록 사이의 균형을 유지한다.
- 전문적인 정보를 전달하되 어렵거나 권위적으로 보이지 않아야 한다.

### 1.3 지양하는 인상

- 실제 대한민국 여권의 문장, 보안 무늬, 번호 규칙, 관용 문구를 복제하지 않는다.
- 정부 발행물이나 공식 신분증으로 오인될 표현을 사용하지 않는다.
- 빈티지 여행 포스터, 국기, 랜드마크, 민족적 문양을 장식적으로 남용하지 않는다.
- 도시를 하나의 색이나 고정관념으로 단순화하거나 과도하게 이국화하지 않는다.
- 음원 앱의 앨범 그리드, 차트, 재생 큐를 그대로 모방하지 않는다.

## 2. 경험 및 UX 원칙

### 2.1 Destination First

추천 경험의 주인공은 Spotify나 개별 곡이 아니라 **Destination**이다. 목적지는 선택 단계에서 가장 강한 시각적 도착점으로 제시하고, 결과의 헤드라인과 스탬프에서도 가장 먼저 인지되게 한다.

### 2.2 One Decision at a Time

Mood, Situation, Destination을 한 번에 모두 요구하지 않고 한 단계에 하나의 질문을 제시한다. 현재 단계, 완료된 선택, 다음 행동이 언제나 명확해야 한다.

### 2.3 Curated, Not Endless

무한 스크롤이나 과도한 선택지를 피한다. 결과는 정확히 5곡으로 제한하고, `01–05` 번호와 충분한 여백을 통해 큐레이터가 구성한 하나의 흐름으로 보이게 한다.

### 2.4 Context Before Playback

곡명과 아티스트뿐 아니라 도시와의 관계, 현재 Mood/Situation에 어울리는 이유를 짧게 전달한다. 재생 버튼은 중요하지만 문화적 맥락을 압도하지 않는다.

### 2.5 Tactile, Not Decorative

종이 질감, 카드의 겹침, 스탬프는 정보의 층위와 여행 서사를 강화할 때만 사용한다. 질감은 텍스트 가독성을 해치지 않는 낮은 강도로 적용한다.

### 2.6 Calm Motion

애니메이션은 상태 변화와 공간 관계를 설명하는 용도로만 사용한다. 여권이 과장되게 날아오거나 스탬프가 반복해서 찍히는 연출, 장시간의 패럴랙스와 자동 재생 효과는 사용하지 않는다.

### 2.7 Accessible by Default

색상 없이도 제목, 번호, 테두리, 아이콘, 텍스트로 구조와 상태를 이해할 수 있어야 한다. 키보드, 스크린 리더, 확대 화면과 모션 감소 설정을 기본 설계 범위에 포함한다.

## 3. 정보 구조와 전체 여정

### 3.1 기본 흐름

1. **Home:** 서비스 가치와 시작 행동 소개
2. **Mood 선택:** 현재 감정 한 가지 선택
3. **Situation 선택:** 음악을 들을 상황 한 가지 선택
4. **Destination 선택:** 음악으로 여행할 도시 한 곳 선택
5. **Passport 결과:** 선택 요약, 발급 정보, 도시의 Music Scene, Travel Stamp 제공
6. **Playlist Card:** 01~05 트랙과 설명, Spotify 외부 감상 동선 제공

### 3.2 공통 화면 골격

- **Global Header:** 워드마크, 현재 여정 상태, 필요한 경우 처음으로 이동
- **Journey Indicator:** `01 Mood / 02 Situation / 03 Destination` 진행 상태
- **Primary Content:** 한 화면에 하나의 핵심 질문 또는 결과
- **Context Area:** 짧은 도움말, 선택 요약, 문화적 안내
- **Action Area:** 이전/다음 또는 여권 발급 CTA
- **Footer:** 서비스 성격 및 외부 서비스 고지 등 최소 정보

데스크톱은 중앙 정렬된 최대 너비 컨테이너를 사용하고, 모바일은 화면 가장자리의 안전 여백을 유지한 단일 열을 기본으로 한다.

## 4. 화면별 UI/UX 가이드

### 4.1 Home

#### 목적

첫 화면에서 “음악으로 도시를 여행하고 5곡의 여권을 받는 서비스”임을 즉시 이해시키고 선택 여정으로 안내한다.

#### 권장 구성

1. 작은 에디토리얼 라벨: `MUSIC PASSPORT / ISSUE 001`
2. 헤드라인: “오늘은 어떤 음악의 도시로 떠나볼까요?”
3. 1~2문장의 가치 설명
4. 주요 CTA: “음악 여행 시작하기”
5. 여권과 Playlist Card를 추상화한 정적인 미리보기
6. `기분 · 상황 · 목적지 · 5곡`으로 과정 요약

CTA는 첫 뷰포트 안에서 찾을 수 있어야 한다. 배경 영상, 자동 재생 음악, 목적지 사진 슬라이더는 사용하지 않는다.

### 4.2 Mood 선택

#### 질문

“지금 어떤 기분으로 떠나고 싶나요?”

#### 선택지

차분한(Calm), 활기찬(Energetic), 쓸쓸한(Melancholic), 로맨틱한(Romantic), 모험적인(Adventurous)

#### UI 패턴

- 단일 선택 카드 또는 큰 라디오 그룹을 사용한다.
- 각 항목은 한국어 이름, 영문 보조명, 한 줄 설명으로 구성한다.
- 선택 상태는 테두리 굵기, 체크 표시, 배경 변화 및 `선택됨` 상태 텍스트를 조합한다.
- 선택 즉시 다음 단계로 강제 이동하지 않고, 사용자가 CTA로 확정하도록 한다.

### 4.3 Situation 선택

#### 질문

“어떤 순간에 이 음악을 들을까요?”

#### 선택지

아침 산책(Morning Walk), 출퇴근(Commuting), 업무·공부(Working), 비 오는 저녁(Rainy Evening), 늦은 밤(Late Night)

#### UI 패턴

Mood와 동일한 선택 문법을 유지한다. 상황을 묘사하는 장식 사진보다 간결한 선형 아이콘과 텍스트를 사용해 선택 속도를 높인다.

### 4.4 Destination 선택

#### 질문

“이번 음악 여행의 목적지는 어디인가요?”

#### 선택지

London, Seoul, Tokyo, Paris, Berlin, São Paulo, Lagos, New York

#### UI 패턴

- 데스크톱에서는 2~4열 도시 카드, 모바일에서는 1~2열 또는 세로 목록을 사용한다.
- 도시명은 가장 크게, 국가/지역과 음악 신 힌트는 보조 정보로 제공한다.
- 국기만으로 도시를 식별하지 않으며 국기 이모지를 핵심 아이콘으로 쓰지 않는다.
- 특정 도시를 관광 명소 하나로 대표하지 않는다.
- 하단에 Mood와 Situation 선택 요약을 제공한다.
- 최종 CTA는 “음악 여권 발급하기”로 명확히 표현한다.

### 4.5 로딩 및 발급 중 상태

- 메시지: “선택한 도시의 음악 신을 살펴보고 있어요.”
- 여권 번호 라인이나 5개의 트랙 슬롯이 채워지는 정적인 스켈레톤을 사용한다.
- 중복 실행을 막기 위해 CTA를 비활성화하고 `발급 중` 텍스트를 함께 표시한다.
- 진행률을 정확히 알 수 없으면 거짓 퍼센트나 카운트다운을 제공하지 않는다.
- 모션은 짧은 페이드 또는 낮은 강도의 펄스로 제한하며 `prefers-reduced-motion`에서는 제거한다.

### 4.6 Passport 결과 화면

결과의 첫 인상은 “추천 목록”이 아니라 “한 도시의 음악 여권이 발급되었다”여야 한다.

#### 권장 순서

1. 발급 완료 라벨과 Destination 헤드라인
2. Passport 펼침 영역
3. Travel Stamp
4. 도시 Music Scene 요약
5. Playlist Card 01~05
6. 다른 조건으로 다시 여행하는 보조 CTA

데스크톱에서는 Passport와 Playlist Card를 시각적으로 겹치거나 나란히 둘 수 있다. 모바일에서는 Passport 정보를 먼저 읽고 트랙으로 자연스럽게 이어지는 단일 열로 재배치한다.

### 4.7 오류 및 빈 상태

- **선택 누락:** 누락된 단계 이름과 해결 방법을 해당 필드 가까이에 표시하고 포커스를 이동한다.
- **발급 실패:** “여권을 발급하지 못했어요. 잠시 후 다시 시도해 주세요.”와 `다시 시도`/`선택으로 돌아가기`를 제공한다.
- **빈 결과:** 도시 연관성이 확인된 5곡을 준비하지 못했음을 알리고 다른 조건을 선택할 수 있게 한다.
- **Spotify 링크 없음:** 버튼을 숨겨 공간을 모호하게 만들기보다 `현재 Spotify 링크를 확인할 수 없어요`라는 비활성 상태를 표시한다.
- 내부 예외, API 명칭, 스택 트레이스는 사용자에게 노출하지 않는다.

## 5. Passport 컴포넌트

### 5.1 시각 구조

Passport는 펼쳐진 2면 인쇄물에서 영감을 얻되 실제 신분증 레이아웃을 복제하지 않는다.

- **표지/좌측 면:** 서비스 워드마크, Destination, 짧은 여행 문장 또는 Music Scene 요약
- **정보/우측 면:** Mood, Situation, Music Scene, Issue Date, Passport ID
- **Stamp Layer:** 정보 영역의 가장자리 또는 지정된 스탬프 구역
- **Card Pocket:** Playlist Card가 끼워진 듯한 낮은 깊이의 레이어

### 5.2 필수 정보와 위계

| 우선순위 | 필드 | 권장 표현 |
|---|---|---|
| 1 | Destination | 가장 큰 제목, 원어 도시명 우선 |
| 2 | Music Scene | 1~3단어 분류 + 짧은 문화적 설명 |
| 3 | Mood / Situation | 각각 레이블과 선택값을 한 쌍으로 표시 |
| 4 | Issue Date | 지역 혼동이 없는 `YYYY.MM.DD` 형식 권장 |
| 5 | Passport ID | `MP-[CITY]-[DATE]-[RANDOM]` 형태의 비민감 식별자 |
| 6 | Travel Stamp | 도시명, 발급일, 창작 그래픽을 포함한 장식적 보조 요소 |

Passport ID는 개인정보, 실제 여권번호 패턴 또는 추적 가능한 사용자 식별자를 포함하지 않는다. 화면 어딘가에 `Music Passport가 발급한 음악 여행 기록`임을 알 수 있게 해 공식 문서 오인을 방지한다.

### 5.3 Travel Stamp

- 원형, 타원형, 직사각형 등 도시별로 구분 가능한 단순 기하 형태를 사용한다.
- 도시명, 날짜, `MUSIC ENTRY` 같은 서비스 고유 문구를 조합한다.
- 약간의 잉크 번짐과 회전은 허용하되 작은 글자의 판독성을 유지한다.
- 본문 위를 가로지르지 않으며, 핵심 정보와 최소 안전 간격을 둔다.
- 도시의 문화적 맥락을 검토한 창작 모티프만 사용하고 공공기관의 문장이나 입국 도장을 복제하지 않는다.
- 스탬프의 회전·색·형태만으로 방문 여부를 전달하지 않고 텍스트 레이블을 병기한다.

## 6. Playlist Card 컴포넌트

FRITZ의 플레이리스트 카드에서 느껴지는 번호 중심의 인쇄물·에디토리얼 감도를 참고하되, 고유한 Music Passport 시스템으로 재해석한다.

### 6.1 카드 구성

각 카드 또는 행은 다음을 포함한다.

1. **Track Number:** `01`부터 `05`까지 두 자리 표기
2. **Song Title:** 트랙의 주요 제목
3. **Artist:** 곡명과 명확히 구분되는 보조 제목
4. **Short Description:** 도시 연관성과 Mood/Situation 적합 이유를 간결하게 설명
5. **Spotify Button:** 외부 재생 동선임을 명시

장르 또는 Music Scene 태그는 선택적으로 추가할 수 있다. 향후 QR 코드 영역을 추가할 수 있도록 트랙 메타데이터와 액션 영역을 분리한다.

### 6.2 레이아웃

- 데스크톱: 번호 열 / 곡 정보 열 / 설명 열 / 액션 열의 에디토리얼 그리드
- 태블릿: 번호와 곡 정보를 상단에 두고 설명과 액션을 다음 행에 배치
- 모바일: 번호 → 곡명·아티스트 → 설명 → 전체 너비 버튼 순의 단일 열
- 카드 사이에는 과한 그림자 대신 헤어라인, 넉넉한 패딩, 종이 겹침을 암시하는 미세한 음영을 사용한다.
- 5개 트랙은 동일한 구조를 유지하며 번호가 스크롤의 리듬을 만든다.

### 6.3 Spotify 버튼

- 레이블: `Spotify에서 듣기`와 외부 링크 아이콘
- **Spotify Green은 이 버튼의 배경, 아이콘 또는 명확한 재생 액센트에만 사용한다.**
- Music Passport의 CTA, 선택 상태, 스탬프, 링크 등 다른 요소에는 Spotify Green을 사용하지 않는다.
- 브랜드 로고 사용, 최소 크기, 여백, 명칭은 구현 시점의 Spotify 공식 브랜드 가이드를 확인한다.
- 새 탭 또는 앱으로 이동하는 외부 동선임을 텍스트와 접근 가능한 이름으로 알린다.
- 버튼 전체에 최소 터치 영역을 적용하고 키보드 포커스를 명확히 표시한다.

## 7. 레이아웃 시스템

### 7.1 그리드

| 구간 | 권장 그리드 | 바깥 여백 | 콘텐츠 전략 |
|---|---|---|---|
| Mobile (`< 768px`) | 4 columns | 16–24px | 단일 열, 필수 정보 우선 |
| Tablet (`768–1199px`) | 8 columns | 32–48px | 선택 카드 2열, 결과 순차 배치 |
| Desktop (`≥ 1200px`) | 12 columns | 48–80px | 최대 너비 컨테이너, Passport 2면 구성 |

중단점 수치와 최대 너비는 구현 환경과 실제 콘텐츠 테스트에 따라 조정할 수 있다. 화면 폭보다 콘텐츠의 줄 길이와 정보 밀도를 우선한다.

### 7.2 여백

- 4px 기반의 간격 체계를 사용하되 주요 배치는 8px 배수로 구성한다.
- 섹션 사이에는 카드 내부 간격보다 최소 2배 큰 여백을 둔다.
- 본문 줄 길이는 데스크톱에서 한글 기준 약 35~55자를 권장한다.
- 빈 공간을 장식으로 채우지 않는다. White Space는 단계 구분과 정보 위계를 만드는 핵심 요소다.

### 7.3 표면과 깊이

- 기본 배경, Passport paper, Playlist paper의 톤 또는 질감을 미세하게 달리해 층위를 만든다.
- 그림자는 넓고 옅게 사용하며 떠 있는 앱 패널보다 겹친 종이에 가깝게 표현한다.
- 모서리는 지나치게 둥글게 만들지 않는다. Passport와 인쇄물의 단정한 형태에 맞춰 작은 반경 또는 직각을 기본으로 한다.

## 8. 디자인 토큰 제안

색상은 확정하지 않는다. 아래 값은 구조와 용도를 검증하기 위한 **임시값**이며, 브랜드 검토와 대비 테스트 후 교체해야 한다. 토큰 이름은 실제 색 이름이 아니라 의미와 역할을 기준으로 한다.

### 8.1 Color tokens — 임시값

```text
color.bg.canvas              = #F5F2EA  // 전체 종이 배경
color.bg.surface             = #FFFDF8  // 기본 표면
color.bg.passport            = #ECE8DE  // Passport 내지
color.bg.inverse             = #20231F  // 역상 영역

color.text.primary           = #20231F
color.text.secondary         = #61655E
color.text.muted             = #858980
color.text.inverse           = #FFFDF8

color.border.subtle          = #D9D5CA
color.border.strong          = #777B73
color.focus                  = #315E73

color.brand.primary          = #243F3A  // 임시 브랜드 주조색
color.brand.secondary        = #9B5B48  // 임시 스탬프/에디토리얼 보조색
color.brand.on-primary       = #FFFDF8

color.action.spotify         = #1DB954  // Spotify 액션 전용 임시값
color.action.on-spotify      = #0B0B0B

color.state.success          = #3E6B50
color.state.warning          = #8B641D
color.state.error            = #9A3E39
color.state.disabled         = #B7BAB3
```

색상 적용 시 WCAG 대비 기준을 확인한다. 본문 텍스트는 최소 4.5:1, 큰 텍스트와 필수 UI 경계는 최소 3:1을 목표로 한다. 상태는 색과 함께 아이콘, 문구, 테두리 패턴을 제공한다.

### 8.2 Typography tokens

```text
font.family.display          = "Recommended Display Serif", serif
font.family.body             = "Recommended Korean Sans", sans-serif
font.family.label            = "Recommended Korean Sans", sans-serif
font.family.mono             = "Recommended Mono", monospace

font.size.display            = clamp(2.5rem, 7vw, 5.5rem)
font.size.heading-1          = clamp(2rem, 4vw, 3.5rem)
font.size.heading-2          = clamp(1.5rem, 3vw, 2.25rem)
font.size.heading-3          = 1.25rem
font.size.body-lg            = 1.125rem
font.size.body               = 1rem
font.size.body-sm            = 0.875rem
font.size.label              = 0.75rem

font.weight.regular          = 400
font.weight.medium           = 500
font.weight.semibold         = 600
font.weight.bold             = 700

line-height.tight            = 1.15
line-height.heading          = 1.3
line-height.body             = 1.65
letter-spacing.label         = 0.08em
```

### 8.3 Spacing, radius, border, shadow tokens

```text
space.0  = 0
space.1  = 4px
space.2  = 8px
space.3  = 12px
space.4  = 16px
space.6  = 24px
space.8  = 32px
space.12 = 48px
space.16 = 64px
space.24 = 96px

radius.none = 0
radius.sm   = 2px
radius.md   = 6px
radius.pill = 999px

border.hairline = 1px
border.active   = 2px

shadow.paper-1 = 0 2px 8px rgba(32, 35, 31, 0.08)
shadow.paper-2 = 0 12px 32px rgba(32, 35, 31, 0.12)
```

### 8.4 Motion tokens

```text
motion.duration.fast     = 120ms
motion.duration.standard = 200ms
motion.duration.slow     = 320ms
motion.easing.standard   = cubic-bezier(0.2, 0, 0, 1)
motion.distance.small    = 4px
```

모든 움직임은 `prefers-reduced-motion`에서 제거하거나 즉시 전환으로 대체한다. 사용자 입력에 대한 피드백은 빠르게, 결과의 종이 레이어 전환은 320ms 이내로 제한한다.

## 9. Typography 가이드

### 9.1 서체 방향

- **제목:** 절제된 디스플레이 세리프 또는 세리프 성격이 약한 현대적 명조. 목적지와 에디토리얼 헤드라인에 사용한다.
- **본문:** 작은 화면에서도 선명한 한국어 산세리프. 긴 설명, 선택지, 상태 메시지에 사용한다.
- **카드:** 트랙 번호에는 산세리프 볼드 또는 모노스페이스, 곡명에는 제목 계열, 설명에는 본문 계열을 조합한다.
- **버튼:** 본문 산세리프 Medium/Semibold를 사용하며 대문자·과도한 자간을 피한다.

실제 서체는 한글·라틴 문자·숫자의 조화, 웹 라이선스, 로딩 성능을 검토한 뒤 확정한다. 장식적인 필기체는 스탬프의 제한된 그래픽을 제외하고 사용하지 않는다.

### 9.2 역할별 권장 스타일

| 역할 | 크기/행간 방향 | 굵기 | 사용 예 |
|---|---|---|---|
| Display | 반응형 40–88px / 1.15 | Regular–Semibold | Home 슬로건, Destination |
| Heading 1 | 32–56px / 1.2 | Semibold | 화면 질문, 결과 제목 |
| Heading 2 | 24–36px / 1.3 | Semibold | Passport/Playlist 섹션 |
| Body | 16–18px / 1.6–1.7 | Regular | 설명, 추천 이유 |
| Card Title | 18–24px / 1.3 | Semibold | Song Title |
| Card Meta | 14–16px / 1.5 | Medium | Artist, Music Scene |
| Label | 12–14px / 1.4 | Semibold | 필드명, 단계, 날짜 |
| Button | 15–16px / 1 | Semibold | CTA, Spotify 버튼 |

본문과 버튼 텍스트는 모바일에서도 16px을 기본으로 한다. 보조 레이블은 12px 미만으로 축소하지 않는다. 영문 대문자 라벨에는 자간을 사용할 수 있지만 한국어에는 과도한 자간을 적용하지 않는다.

## 10. 아이콘 가이드

### 10.1 권장 스타일

- 1.5~2px의 균일한 선 굵기를 가진 단순한 **outline icon**을 기본으로 한다.
- 둥근 끝과 절제된 기하 형태를 사용해 현대적이고 친근한 인상을 유지한다.
- 채움 아이콘은 선택 완료, 오류 등 강한 상태 표시에 제한한다.
- 한 화면에서 서로 다른 아이콘 패밀리를 혼용하지 않는다.
- 여권, 나침반, 티켓, 헤드폰, 산책, 우산 등 의미가 명확한 보편적 은유를 사용한다.

### 10.2 사용 원칙

- 아이콘만으로 핵심 동작을 표시하지 않고 항상 텍스트 레이블 또는 접근 가능한 이름을 제공한다.
- 장식 아이콘은 스크린 리더에서 제외한다.
- 기본 크기는 본문용 20–24px, 주요 행동용 24px을 권장하며 터치 영역은 아이콘보다 크게 확보한다.
- 국기, 종교·민족 상징, 정부 문장, 실제 입국 심사 마크는 일반 아이콘으로 사용하지 않는다.
- Spotify에는 공식 가이드에 맞는 로고 또는 재생 아이콘만 사용하고 자체 브랜드 아이콘과 구분한다.

## 11. 컴포넌트 상태와 인터랙션

### 11.1 선택 카드

- **Default:** 얇은 테두리, 중립 배경
- **Hover:** 작은 배경/테두리 변화만 적용하며 위치를 크게 이동하지 않음
- **Focus:** 2px 이상의 명확한 외곽 포커스 링
- **Selected:** 굵은 테두리 + 체크 아이콘 + `선택됨` 접근성 상태
- **Disabled:** 대비를 낮추되 텍스트를 읽을 수 있고 비활성 이유를 제공
- **Error:** 오류 아이콘과 해결 문구를 함께 표시

카드 전체를 클릭할 수 있게 하되 내부 의미 구조는 라디오 그룹으로 제공한다.

### 11.2 버튼

- Primary는 단계당 하나만 두고 다음 행동을 동사형 문구로 표현한다.
- Secondary는 이전 단계, 다시 선택 등 복구 동선에 사용한다.
- Spotify 버튼은 별도 외부 액션 변형으로 관리한다.
- 최소 터치 영역은 44×44px 이상을 권장한다.
- 로딩 중에는 폭이 변하지 않도록 레이블 공간을 유지한다.
- hover에만 의존하는 정보나 동작을 제공하지 않는다.

### 11.3 링크

본문 링크는 색뿐 아니라 밑줄 또는 다른 비색상 표식을 사용한다. 외부 링크에는 아이콘과 `새 창` 또는 서비스명을 포함한 접근 가능한 설명을 제공한다.

## 12. 종이 질감과 이미지 사용

### 12.1 Paper Texture

- 1~3% 수준의 미세한 노이즈, 섬유 또는 점 패턴을 권장한다.
- 텍스트 바로 아래에는 질감을 더 약하게 적용한다.
- 고해상도 이미지 한 장보다 반복 가능한 가벼운 패턴이나 CSS 기반 질감을 우선 검토한다.
- 다크 모드 또는 고대비 모드에서 질감이 얼룩이나 오류 상태로 보이지 않게 한다.
- 질감을 제거해도 정보 구조가 완전해야 한다.

### 12.2 도시 이미지

MVP에서는 사진이 필수가 아니다. 사용할 경우 관광 명소보다 레코드숍, 공연장, 거리의 음악 문화처럼 맥락 있는 이미지를 우선하며, 출처·저작권·현지 표현을 검토한다. 이미지 없이도 타이포그래피와 스탬프 시스템만으로 도시를 구분할 수 있어야 한다.

## 13. 반응형 설계

### 13.1 Desktop

- 최대 너비 컨테이너 안에서 여백이 넉넉한 12열 그리드를 사용한다.
- 선택 화면은 질문 영역과 선택 영역을 분할할 수 있다.
- Passport 결과는 2면 펼침 또는 Passport/Playlist 병렬 구성을 허용한다.
- Playlist Card는 4개 정보 열을 사용하되 설명의 줄 길이를 제한한다.
- 마우스 hover는 보조 피드백으로만 제공한다.

### 13.2 Mobile

- 모든 핵심 정보는 가로 스크롤 없이 단일 열로 읽을 수 있어야 한다.
- 헤드라인 크기를 줄이되 Destination의 우선순위를 유지한다.
- 선택 카드는 큰 터치 영역과 16px 이상의 본문을 사용한다.
- 이전/다음 버튼은 엄지손가락 접근성을 고려해 화면 하단에 둘 수 있으나 콘텐츠를 가리지 않는다.
- Passport의 좌우 면을 억지로 축소하지 않고 순서대로 쌓는다.
- Playlist Card의 Spotify 버튼은 가능한 경우 전체 너비로 제공한다.
- 스탬프는 본문을 침범하지 않는 별도 영역으로 이동한다.

### 13.3 긴 콘텐츠와 현지화 대응

- 긴 곡명과 아티스트명은 임의로 생략하지 않고 자연스럽게 줄바꿈한다.
- `São Paulo`처럼 발음 구별 기호가 있는 고유명사를 정확히 표시한다.
- 한글 병기 시 두 언어의 위계를 정하고 괄호가 반복되어 복잡해지지 않게 한다.
- 200% 확대에서도 핵심 흐름과 액션이 겹치거나 잘리지 않아야 한다.

## 14. 접근성 체크리스트

- 모든 선택지는 키보드로 이동·선택할 수 있고 논리적 포커스 순서를 갖는다.
- 진행 단계와 현재 단계를 시각 정보뿐 아니라 텍스트/ARIA 상태로 전달한다.
- 포커스 표시를 제거하지 않으며 배경과 충분한 대비를 확보한다.
- 본문 대비 4.5:1 이상, 큰 텍스트·UI 경계 3:1 이상을 목표로 한다.
- 색상은 선택, 성공, 오류, 비활성 상태의 유일한 단서가 아니다.
- 아이콘 버튼과 외부 링크에 명확한 접근 가능한 이름을 제공한다.
- 동적 로딩·성공·오류 메시지는 보조 기술에 적절히 공지한다.
- `prefers-reduced-motion`을 존중하고 자동 재생 오디오/비디오를 사용하지 않는다.
- 터치 대상은 최소 44×44px을 권장하고 인접 대상 사이에 충분한 간격을 둔다.
- 종이 질감과 스탬프는 텍스트를 가리지 않으며 장식 요소는 읽기 순서에서 제외한다.

## 15. 콘텐츠와 마이크로카피

### 15.1 톤

- 친절하고 간결한 한국어 존댓말을 사용한다.
- 음악적 전문성은 구체적인 문화 정보로 드러내고 과장된 수식은 피한다.
- 도시에 대한 고정관념, 단정, 이국화를 피한다.
- 곡과 도시의 관계를 설명할 때 확인 가능한 사실과 감상적 표현을 구분한다.

### 15.2 권장 문구

| 상황 | 문구 |
|---|---|
| Home | “오늘은 어떤 음악의 도시로 떠나볼까요?” |
| 시작 CTA | “음악 여행 시작하기” |
| 최종 CTA | “음악 여권 발급하기” |
| 로딩 | “선택한 도시의 음악 신을 살펴보고 있어요.” |
| Spotify | “Spotify에서 듣기” |
| 재탐색 | “다른 도시로 떠나기” |
| 오류 | “여권을 발급하지 못했어요. 잠시 후 다시 시도해 주세요.” |

CTA는 `확인`, `계속` 같은 모호한 표현보다 다음 결과를 예측할 수 있는 동사를 사용한다.

## 16. 디자인 QA 기준

### 16.1 시각 QA

- 색상을 제거해도 단계, 선택, 정보 위계가 구분되는가?
- Passport가 실제 정부 문서가 아니라 창작된 음악 여행 기록으로 보이는가?
- Spotify Green이 재생 동선 외에는 사용되지 않았는가?
- 종이 질감과 스탬프가 본문의 판독성을 해치지 않는가?
- 다섯 트랙의 번호와 읽기 흐름이 명확한가?
- White Space가 화면 크기별로 충분한가?

### 16.2 UX QA

- 사용자가 현재 단계와 남은 단계를 이해할 수 있는가?
- Mood, Situation, Destination을 각각 정확히 하나만 선택할 수 있는가?
- 선택 누락과 API 실패에서 다음 행동이 분명한가?
- Spotify가 외부 동선임을 클릭 전에 이해할 수 있는가?
- 결과에서 입력 단계로 안전하게 돌아갈 수 있는가?

### 16.3 반응형·접근성 QA

- 주요 모바일·데스크톱 너비에서 가로 스크롤이나 겹침이 없는가?
- 키보드만으로 전체 흐름을 완료할 수 있는가?
- 200% 확대와 긴 제목에서도 정보가 잘리지 않는가?
- 터치 영역, 대비, 포커스, 상태 공지가 기준을 충족하는가?
- 모션 감소 설정에서 의미 손실 없이 사용할 수 있는가?

## 17. MVP 우선순위와 후속 결정

### 17.1 MVP에서 우선 확정할 것

1. 선택 화면의 단일 선택 패턴과 진행 표시
2. Passport 필수 필드의 위계와 모바일 재배치
3. 01~05 Playlist Card 반복 구조
4. Spotify 전용 액션 스타일
5. 로딩, 누락, 빈 결과, 오류의 복구 동선
6. 키보드·대비·터치 영역 등 기본 접근성

### 17.2 후속 검증이 필요한 것

- 최종 브랜드 컬러와 도시별 스탬프 팔레트
- 한글/라틴 디스플레이 및 본문 서체 조합
- 종이 질감의 제작 방식과 성능 영향
- Streamlit에서 가능한 Passport 펼침/겹침 표현 수준
- 도시 이미지 사용 여부와 콘텐츠 권리 정책
- QR 코드 추가 시 Playlist Card 레이아웃
- 실제 사용자 테스트에 따른 단계 분리와 CTA 위치

색상, 서체, 질감, 모션의 최종값은 브랜드 검토, 실제 콘텐츠 적용, 접근성 및 성능 테스트 이후 결정한다. 어떤 시각 표현을 선택하더라도 **도시 중심의 문화적 발견**, **다섯 곡의 편집된 경험**, **명확한 정보 구조**라는 핵심 원칙을 우선한다.
